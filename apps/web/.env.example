# The web zone's tRPC route handler runs procedures that query the database,
# so it needs the connection string. Copy this to `.env` for local dev.
# Local dev uses the Docker Postgres from the repo-root docker-compose.yml.
DATABASE_URL=postgres://pricy:pricy@localhost:5432/pricy
